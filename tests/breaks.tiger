// 55
let
    function simple_loop(n: int) =
        let var a := 0 in
            for i := 1 to 100 do if a = n then break else a := a + i;
            a
            end
in
    print_int(simple_loop(20))
end
